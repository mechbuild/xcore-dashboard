import React from 'react';
import ReactDOM from 'react-dom/client';
import XCoreDashboard from './XCoreDashboard';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <XCoreDashboard />
  </React.StrictMode>
);